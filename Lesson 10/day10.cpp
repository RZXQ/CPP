#include <iostream>

using namespace std;

int main() {
    // 练习逻辑运算符非!
    // cout << !true << endl;
    // cout << !false << endl;
    // cout << !(4 > 3) << endl;
    // cout << !(4 > 6) << endl;

    // 练习逻辑运算符与 &&
    // cout << (9 > 5 && 5 > 3) << endl;
    // cout << (9 > 5 && 5 < 3) << endl;
    // cout << (9 < 5 && 5 < 3) << endl;

    // 判断三条线段是否能构成一个三角形
    // 声明三个整形变量, 并输入
    // int a, b, c;
    // cin >> a >> b >> c;
    // 使用 if... else... 语句进行判断
    // if (a + b > c && b + c > a && a + c > b) {
    //     cout << "yes";
    // } else {
    //     cout << "no";
    // }

    // 练习逻辑运算符或 ||
    // cout << (7 > 4 || 3 > 1) << endl;
    // cout << (4 > 2 || 3 < 5) << endl;
    // cout << (6 > 8 || 7 > 9) << endl;

    // 判断一门成绩不及格
    // 声明两个变量, 并输入
    // int chinese, math;
    // cin >> chinese >> math;
    // 使用 if...else... 语句进行判断
    // if ((chinese >= 60 && math < 60) || (chinese < 60 && math >= 60)) {
    //     cout << 1;
    // } else {
    //     cout << 0;
    // }

    // 判断是否是闰年
    // 声明整形变量, 输入
    // int y;
    // cin >> y;
    // 使用 if...else... 语句进行判断
    // if ((y % 400 == 0) || (y % 100 != 0 && y % 4 == 0)) {
    //     cout << "Yes";
    // } else {
    //     cout << "No";
    // }
    // return 0;
}
