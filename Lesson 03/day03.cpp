#include <iostream>

using namespace std;

int main() {
    // 交换两个变量的值(一)
    // 声明三个变量
    // int a = 3, b = 5, c;
    // 交换两个变量的值
    //    c = a;
    //    a = b;
    //    b = c;
    // 输出
    // cout << a << " " << b << endl;

    // 交换两个变量的值(二)
    // 声明两个变量, 并赋值
    // int a = 3, b = 5;
    // 交换两个变量的值
    // a = a + b;
    // b = a - b;
    // a = a - b;
    // 输出
    // cout << a << " " << b << endl;

    // 交换两个变量的值(三)
    // 使用 swap()方法交换两个变量的值
    // swap(a, b);
    // 输出
    // cout << a << " " << b << endl;

    // 输入三个变量的值, 并输出
    int a, b, c;
    // 输入变量的值
    cin >> a >> b >> c;
    // 输出
    cout << a << " " << b << " " << c << endl;

    //
    return 0;
}