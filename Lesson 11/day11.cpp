#include <iostream>

using namespace std;

int main() {
    // 练习三目运算符
    // cout << (3 > 2 ? 4 : 5); // 4
    // cout << (4 > 5 ? 4 : 5); // 5

    // 使用三目运算符比较两个数的大小
    // int a, b;
    // cin >> a >> b;
    // cout << (a > b ? a : b) << endl;

    // 使用三目运算符比较三个数中最大的数并输出
    // int a, b, c;
    // cin >> a >> b >> c;
    // cout << (a > b ? (a > c ? a : c) : (b > c ? b : c));

    // 使用三目运算符比较三个数中最小的数并输出
    // int a, b, c;
    // cin >> a >> b >> c;
    // cout << (a < b ? (a < c ? a : c) : (b < c ? b : c));

    return 0;
}