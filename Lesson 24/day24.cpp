#include <iostream>
#include <algorithm>
#include <cmath>

using namespace std;

int main() {
    // // sort 函数
    // int n, a[101] = {};
    // cin >> n;
    // for (int i = 0; i < n; i++) {
    //     cin >> a[i];
    // }
    // sort(a, a + n);
    // for (int i = 0; i < n; i++) {
    //     cout << a[i] << " ";
    // }

    // // 温度排列: 使用 sort 函数对温度值从小到大进行排序
    // int n, a[101] = {};
    // cin >> n;
    // for (int i = 0; i < n; i++) {
    //     cin >> a[i];
    // }
    // sort(a, a + n);
    // for (int i = 0; i < n; i++) {
    //     cout << a[i] << " ";
    // }

    // // 计算整数的绝对值
    // int n;
    // cin >> n;
    // int res = 0;
    // res = abs(n);
    // cout << "该数的绝对值为: " << res << endl;

    // // 计算整数的平方根
    // double n;
    // cin >> n;
    // cout << sqrt(n) << endl;

    return 0;
}