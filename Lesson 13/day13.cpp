#include <iostream>

using namespace std;

int main() {
    // 使用 while 循环输出 1 到 100
    // 声明整型变量, 存储变化的数, 循环变量
    // int i = 1;
    // 使用 while 循环循环进行输出
    // while (i <= 100) {
    // 条件为真时重复做的事情
    // cout << i << " ";
    // i = i + 1; // 改变 i 的值
    // }

    // 累加器
    // int i = 1;
    // i++; // i = i + 1;
    // ++i;
    // cout << "i = " << i << endl;

    // i++ 和 ++i 的区别
    // i++ 先使用, 然后在自身的值上加 1
    // ++i 先在自身的值上加 1, 然后再使用
    // int i = 1;
    // cout << ++i << endl; // 2
    // cout << i << endl; // 2
    // cout << i++ << endl; // 1
    // cout << i << endl; // 2

    // 输出 1 - n 之间所有能被 3 整除的数
    // int n, i;
    // cin >> n;
    // 判断
    // if (n < 3) {
    //     cout << 0 << endl;
    // } else { // n >= 3
    // 列举所有的数
    // while (i <= n) {
    //     if (i % 3 == 0) {
    //         cout << i << " ";
    //     }
    //     i++; // 改变 i 的值
    // }
    // }

    // 输出死循环
    // int i = 0; // 计数
    // while (true) {
    //     cout << 6;
    //     i++;
    //     // 判断执行次数是否为 6
    //     if (i == 6) {
    //         break;
    //     }
    // }

    // 数字打印机的程序
    // int n;
    // while (true) {
    //     cin >> n;
    //     // 使用 if 语句判断输入的数字是否0
    //     if (n == 0) {
    //         break;
    //     }
    //     cout << n << endl;
    // }

    // 计算与制定数字相同的数的个数
    // int n, m;
    // int s = 0, temp;
    // cin >> n >> m;
    // while (n--) {
    //     cin >> temp;
    //     // 判断输入的数与指定的数是否相同
    //     if (temp == m) {
    //         s++;
    //     }
    // }
    // // 输出满足条件的数的个数
    // cout << s <<endl;
    return 0;
}
