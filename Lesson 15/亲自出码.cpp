#include <iostream>
#include <iomanip>

using namespace std;

int main() {
    // // 输出 n 个整数的最小值, 最大值, 平均值
    // int n;
    // cin >> n;
    // int sum = 0, max = -1, min = 1001;
    // double avg = 0;
    // int t = n, temp;
    // while (n--) {
    //     cin >> temp;
    //     sum += temp;
    //     // 判断最小值
    //     if (temp < min) {
    //         min = temp;
    //     }
    //     // 判断最大值
    //     if (temp > max) {
    //         max = temp;
    //     }
    // }
    // // 计算平均值
    // avg = 1.0 * sum / t;
    // cout << min << " " << max << " ";
    // cout << fixed << setprecision(3) << avg << endl;

    return 0;
}