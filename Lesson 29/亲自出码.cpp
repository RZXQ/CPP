#include <iostream>
#include <cstring>

using namespace std;

int main() {
    // char flag;
    // string a;
    // int cnt = 0;
    // int l, i;
    // cin >> flag >> a;
    // l = a.length();
    // for (int i = 0; i < l; i++) {
    //     if ((a[i] == flag || a[i] + 32 == flag || a[i] - 32 == flag) &&
    //         (a[i] <= 'z' && a[i] >= 'a' || a[i] <= 'Z' && a[i] >= 'A')) {
    //         cnt++;
    //     }
    // }
    // cout << cnt;
    // return 0;
}