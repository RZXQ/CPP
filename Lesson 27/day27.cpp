#include <iostream>

using namespace std;

int main() {
    // // 求矩形中的最大值及其行号和列号
    // int a[3][4] = {}, max = 0, row = 0, col = 0;
    // // 输入每个元素的值
    // for (int i = 0; i < 3; i++) {
    //     for (int j = 0; j < 4; j++) {
    //         cin >> a[i][j];
    //         if (a[i][j] > max) {
    //             max = a[i][j];
    //             row = i;
    //             col = j;
    //         }
    //     }
    // }
    // // 输出
    // cout << max << endl;
    // cout << row + 1 << " " << col + 1;

    // // 矩形的转置
    // int a[101][101] = {};
    // int n, m;
    // cin >> n >> m;
    // for (int i = 0; i < n; i++) {
    //     for (int j = 0; j < m; j++) {
    //         cin >> a[i][j];
    //     }
    // }
    //
    // for (int i = 0; i < m; i++) {
    //     for (int j = 0; j < n; j++) {
    //         cout << a[j][i] << " ";
    //     }
    //     cout << endl;
    // }

    // // 矩阵的交换
    // int a[5][5] = {};
    // int r1, r2;
    // for (int i = 0; i < 5; i++) {
    //     for (int j = 0; j < 5; j++) {
    //         cin >> a[i][j];
    //     }
    // }
    // cin >> r1 >> r2;
    // // 交换两行的元素
    // for (int i = 0; i < 5; i++) {
    //     swap(a[r1 - 1][i], a[r2 - 2][i]);
    // }
    // // 输出交换完成的二维数组
    // for (int i = 0; i < 5; i++) {
    //     for (int j = 0; j < 5; j++) {
    //         cout << a[i][j] << " ";
    //     }
    //     cout << endl;
    // }

    // return 0;
}